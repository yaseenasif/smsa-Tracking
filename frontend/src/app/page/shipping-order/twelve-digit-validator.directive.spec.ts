import { TwelveDigitValidatorDirective } from './twelve-digit-validator.directive';

describe('TwelveDigitValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new TwelveDigitValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
