import { IsNumberDirective } from './is-number.directive';

describe('IsNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new IsNumberDirective();
    expect(directive).toBeTruthy();
  });
});
