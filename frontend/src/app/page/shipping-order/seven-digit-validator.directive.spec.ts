import { SevenDigitValidatorDirective } from './seven-digit-validator.directive';

describe('SevenDigitValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new SevenDigitValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
